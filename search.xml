<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>我的第一篇安全研究笔记</title>
    <url>/2026/03/17/my-first-blog/</url>
    <content><![CDATA[<h2 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h2><p>这是我正式写下来的第一篇博客。</p>
<p>一直以来，我都在零零散散地学习一些和安全相关的内容，比如 Web 漏洞、抓包分析、CTF 题目和脚本编写，但很多时候只是“学过了”，却没有真正留下记录。时间一久，曾经踩过的坑、看过的知识点、做题时的思路，都会慢慢变得模糊。</p>
<p>所以我想从这篇文章开始，把学习过程认真地记录下来。一方面是为了帮助自己整理思路，另一方面也是希望未来回头看的时候，能清楚地看到自己是怎么一步一步走过来的。</p>
<h2 id="为什么开始学安全"><a href="#为什么开始学安全" class="headerlink" title="为什么开始学安全"></a>为什么开始学安全</h2><p>最开始对安全感兴趣，其实是因为我觉得它很“真实”。</p>
<p>和单纯地学习语法、框架不同，安全更像是在理解一个系统是怎么被搭起来的，又可能会在哪里出问题。你不仅要知道功能是怎么实现的，还要去思考：</p>
<ul>
<li>用户输入会不会被错误地信任</li>
<li>参数传递的边界在哪里</li>
<li>一段看起来正常的逻辑，是否可能被恶意利用</li>
</ul>
<p>这种从“开发视角”切换到“攻击与防御视角”的过程，对我来说很有吸引力。它会逼着我去更深入地理解程序本身，而不是只停留在“会用”。</p>
<h2 id="我目前在学什么"><a href="#我目前在学什么" class="headerlink" title="我目前在学什么"></a>我目前在学什么</h2><p>现阶段我主要把精力放在两个方向：</p>
<h3 id="1-Web-安全基础"><a href="#1-Web-安全基础" class="headerlink" title="1. Web 安全基础"></a>1. Web 安全基础</h3><p>这是我最先接触、也最容易建立反馈的方向。比如：</p>
<ul>
<li>SQL 注入</li>
<li>XSS</li>
<li>文件上传</li>
<li>命令执行</li>
<li>身份认证与权限控制问题</li>
</ul>
<p>这些内容看起来是“漏洞类型”，但本质上是在训练一种思维方式：当一个功能被设计出来后，系统默认相信了什么，又遗漏了什么。</p>
<h3 id="2-CTF-题目练习"><a href="#2-CTF-题目练习" class="headerlink" title="2. CTF 题目练习"></a>2. CTF 题目练习</h3><p>CTF 对我来说很像一种高强度的实战训练。它不会给你完整的背景，也不会一步一步告诉你怎么做，而是需要你自己从题目给出的信息中找线索、做判断、写脚本、验证结果。</p>
<p>这个过程虽然经常会卡住，但也正是最能锻炼问题分析能力的部分。</p>
<h2 id="我希望这类博客记录什么"><a href="#我希望这类博客记录什么" class="headerlink" title="我希望这类博客记录什么"></a>我希望这类博客记录什么</h2><p>从今天开始，这个博客会更偏向“学习笔记 + 过程复盘”，我会尽量记录下面这些内容：</p>
<ul>
<li>某个漏洞原理的理解</li>
<li>做一道题时的思考过程</li>
<li>工具使用中的细节和踩坑</li>
<li>自己写的小脚本与自动化思路</li>
<li>看文章、看源码后的关键收获</li>
</ul>
<p>我不希望它只是把结论抄一遍，而是尽量把“我是怎么想到的、为什么会卡住、最后怎么解决的”写出来。因为很多时候，真正有价值的不是答案本身，而是推到答案的路径。</p>
<h2 id="给现在的自己留一句话"><a href="#给现在的自己留一句话" class="headerlink" title="给现在的自己留一句话"></a>给现在的自己留一句话</h2><p>安全学习很容易让人产生挫败感。</p>
<p>有时候一个环境搭半天跑不起来，有时候一道题看了很久都没有思路，有时候明明知道漏洞名字，却不知道真正落到代码里应该怎么看。这个过程并不轻松，但我也慢慢意识到，很多成长恰恰来自这些“不顺利”的时刻。</p>
<p>所以这篇文章也算是一个小小的起点。先不要求自己一下子写得多专业，也不要求每篇都很完整，先把记录这件事坚持下来。</p>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>这篇博客不算一篇“技术很深”的文章，但它对我来说很重要。</p>
<p>它意味着我不再只是零散地学习，而是开始有意识地整理、复盘和输出。希望未来的自己看到这篇文章时，会感谢现在这个愿意认真开始的自己。</p>
<p>如果你也刚好在学习安全，那就一起慢慢进步。</p>
]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>Web安全</tag>
        <tag>CTF</tag>
        <tag>学习记录</tag>
      </tags>
  </entry>
  <entry>
    <title>CTF Web 入门基础速查</title>
    <url>/2026/03/19/ctf-web-basics-quick-reference/</url>
    <content><![CDATA[<p>这篇文章的目标不是“讲全”，而是先把做 <code>CTF Web</code> 时最常用、最容易卡住的基础点收成一篇能快速翻的速查笔记。</p>
<h2 id="先记住：Web-题本质上在看什么"><a href="#先记住：Web-题本质上在看什么" class="headerlink" title="先记住：Web 题本质上在看什么"></a>先记住：Web 题本质上在看什么</h2><p>大多数 <code>CTF Web</code> 题，绕不开下面几件事：</p>
<ul>
<li>请求是怎么发出去的</li>
<li>参数是怎么传到后端的</li>
<li>数据在中间经历了什么编码和解码</li>
<li>前端有没有泄露线索</li>
<li>后端语言怎么接收和处理输入</li>
<li>登录态、身份信息、权限信息存在哪里</li>
<li>靶机环境到底是主机直跑，还是跑在容器里</li>
</ul>
<p>如果这几件事的链路能看清，很多题就不会只剩“盲试 payload”。</p>
<h2 id="HTTP、URL-与请求响应"><a href="#HTTP、URL-与请求响应" class="headerlink" title="HTTP、URL 与请求响应"></a>HTTP、URL 与请求响应</h2><p><code>Web</code> 题先别急着打 payload，先把请求结构看明白。</p>
<p>一个 URL 常见格式如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">scheme://host:port/path?query#fragment</span><br></pre></td></tr></table></figure>

<p>做题时最该注意的是：</p>
<ul>
<li><code>scheme</code>：<code>http</code> 和 <code>https</code>，决定是否走 TLS。</li>
<li><code>host</code>：域名或 IP，有时题目会藏子域名、虚拟主机。</li>
<li><code>port</code>：不写时 <code>HTTP</code> 默认 <code>80</code>，<code>HTTPS</code> 默认 <code>443</code>。</li>
<li><code>path</code>：资源路径，经常决定路由、文件位置、接口入口。</li>
<li><code>query</code>：问号后面的参数，很多题的输入点就在这里。</li>
<li><code>fragment</code>：<code>#</code> 后的内容不会发给服务器，只在浏览器端使用。</li>
</ul>
<p>最基础的请求&#x2F;响应模型可以理解成：</p>
<ol>
<li>浏览器解析 URL。</li>
<li>建立连接并发请求。</li>
<li>服务端处理后返回响应。</li>
<li>浏览器继续渲染 HTML、加载 CSS、JS、图片等资源。</li>
</ol>
<p>做题时要重点看这几个位置：</p>
<ul>
<li>请求方法：<code>GET</code>、<code>POST</code>、<code>PUT</code>、<code>DELETE</code></li>
<li>请求头：<code>Host</code>、<code>Cookie</code>、<code>Content-Type</code>、<code>User-Agent</code>、<code>Referer</code></li>
<li>请求体：表单、JSON、上传文件</li>
<li>响应头：<code>Set-Cookie</code>、<code>Location</code>、<code>Content-Type</code></li>
<li>状态码：<code>200</code>、<code>302</code>、<code>403</code>、<code>404</code>、<code>500</code></li>
</ul>
<p>一个最小的 <code>POST</code> 请求长这样：</p>
<figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="keyword">POST</span> <span class="string">/login.php</span> <span class="meta">HTTP/1.1</span></span><br><span class="line"><span class="attribute">Host</span><span class="punctuation">: </span>example.com</span><br><span class="line"><span class="attribute">Content-Type</span><span class="punctuation">: </span>application/x-www-form-urlencoded</span><br><span class="line"><span class="attribute">Cookie</span><span class="punctuation">: </span>PHPSESSID=abc123</span><br><span class="line"></span><br><span class="line"><span class="language-ini"><span class="attr">username</span>=admin&amp;password=<span class="number">123456</span></span></span><br></pre></td></tr></table></figure>

<p>在 <code>CTF Web</code> 里，很多信息都藏在“请求哪里不一样”：</p>
<ul>
<li>参数名是不是可控</li>
<li>参数值有没有被过滤</li>
<li>响应长度有没有变化</li>
<li>状态码是不是跳转了</li>
<li>有没有多出来的 <code>Set-Cookie</code></li>
</ul>
<h2 id="编码与进制：很多题不是漏洞难，是你没先解码"><a href="#编码与进制：很多题不是漏洞难，是你没先解码" class="headerlink" title="编码与进制：很多题不是漏洞难，是你没先解码"></a>编码与进制：很多题不是漏洞难，是你没先解码</h2><p>看到奇怪字符串，先别慌，优先怀疑编码。</p>
<p>最常见的几种：</p>
<ul>
<li><code>URL 编码</code>：<code>&lt;</code> 变成 <code>%3C</code>，空格常见为 <code>%20</code></li>
<li><code>HTML 实体</code>：<code>&lt;</code> 变成 <code>&amp;lt;</code></li>
<li><code>Base64</code>：常见由大小写字母、数字、<code>+</code>、<code>/</code>、<code>=</code> 组成</li>
<li><code>Unicode</code>：如 <code>\\u0061</code> 表示 <code>a</code></li>
<li><code>Hex</code>：十六进制，如 <code>61 62 63</code></li>
<li><code>ASCII / UTF-8</code>：字符和字节之间的映射</li>
</ul>
<p>做题里经常要会这几个动作：</p>
<ul>
<li>识别“这是原始数据还是编码后的数据”</li>
<li>判断“被编码了几次”</li>
<li>知道“服务端在哪一层会解码”</li>
</ul>
<p>例如下面几组值，肉眼就该有反应：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">%3Cscript%3Ealert(1)%3C/script%3E</span><br><span class="line">YWRtaW46MTIzNDU2</span><br><span class="line">\u003c?php phpinfo(); ?\u003e</span><br><span class="line">0x61646d696e</span><br></pre></td></tr></table></figure>

<p>它们分别可能对应：</p>
<ul>
<li>URL 编码后的 XSS 负载</li>
<li>Base64 编码后的 <code>admin:123456</code></li>
<li>Unicode 转义后的 PHP 代码片段</li>
<li>十六进制表示的字符串</li>
</ul>
<p>进制本身不复杂，但 <code>CTF Web</code> 很常见：</p>
<ul>
<li>十六进制看字符串：<code>0x616263</code> 就是 <code>abc</code></li>
<li>ASCII 对照回显：十进制、十六进制、字符互转</li>
<li>二进制和位运算偶尔会出现在验证码、加密、校验逻辑里</li>
</ul>
<p>如果页面、接口、参数看起来“像乱码”，优先做下面的检查：</p>
<ul>
<li>有没有 <code>%xx</code></li>
<li>有没有 <code>\\u</code></li>
<li>有没有一长串只由 Base64 字符集组成的内容</li>
<li>有没有十六进制前缀 <code>0x</code></li>
<li>解码一次后是不是还能继续解</li>
</ul>
<h2 id="HTML、CSS、JavaScript：前端不只是样式，还是线索区"><a href="#HTML、CSS、JavaScript：前端不只是样式，还是线索区" class="headerlink" title="HTML、CSS、JavaScript：前端不只是样式，还是线索区"></a>HTML、CSS、JavaScript：前端不只是样式，还是线索区</h2><p>很多新手看到前端代码就跳过，但 <code>Web</code> 题里，前端经常直接送分。</p>
<p>看页面源码时，建议重点看：</p>
<ul>
<li>表单提交到哪里：<code>action</code></li>
<li>提交方式：<code>method</code></li>
<li>隐藏字段：<code>input type=&quot;hidden&quot;</code></li>
<li>注释里的提示</li>
<li>外链 JS 里的接口路径</li>
<li>前端写死的账号、路径、参数名</li>
<li><code>fetch</code>、<code>ajax</code>、<code>axios</code> 请求逻辑</li>
<li>是否有前端校验但后端未必校验</li>
</ul>
<p>一个简单表单示例：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">form</span> <span class="attr">action</span>=<span class="string">&quot;/login.php&quot;</span> <span class="attr">method</span>=<span class="string">&quot;post&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">input</span> <span class="attr">name</span>=<span class="string">&quot;username&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">input</span> <span class="attr">name</span>=<span class="string">&quot;password&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;hidden&quot;</span> <span class="attr">name</span>=<span class="string">&quot;role&quot;</span> <span class="attr">value</span>=<span class="string">&quot;user&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>看到它时，至少该立刻想到：</p>
<ul>
<li>参数名固定为 <code>username</code>、<code>password</code>、<code>role</code></li>
<li><code>role</code> 虽然隐藏，但通常仍然可改</li>
<li>真实入口是 <code>/login.php</code></li>
<li>请求方式是 <code>POST</code></li>
</ul>
<p><code>JavaScript</code> 里常见的题目线索有：</p>
<ul>
<li><code>/api/debug</code></li>
<li><code>/admin</code></li>
<li><code>/flag</code></li>
<li>被注释掉的调试代码</li>
<li>前端校验逻辑</li>
<li>Base64、MD5、AES 之类的前端处理过程</li>
</ul>
<p>一句话概括：前端代码不一定决定漏洞，但它经常决定你该从哪里下手。</p>
<h2 id="PHP：先会看输入从哪进来，再看它怎么被处理"><a href="#PHP：先会看输入从哪进来，再看它怎么被处理" class="headerlink" title="PHP：先会看输入从哪进来，再看它怎么被处理"></a>PHP：先会看输入从哪进来，再看它怎么被处理</h2><p><code>PHP</code> 是 <code>CTF Web</code> 里非常高频的后端语言。基础语法不用背太多，先抓最关键的入口。</p>
<p>最常见的超全局变量：</p>
<ul>
<li><code>$_GET</code>：取 URL 参数</li>
<li><code>$_POST</code>：取请求体参数</li>
<li><code>$_REQUEST</code>：可能同时包含 <code>GET</code>、<code>POST</code>、<code>COOKIE</code></li>
<li><code>$_COOKIE</code>：取 Cookie</li>
<li><code>$_SESSION</code>：取会话数据</li>
<li><code>$_FILES</code>：处理文件上传</li>
</ul>
<p>一个最小例子：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="title function_ invoke__">session_start</span>();</span><br><span class="line"><span class="variable">$name</span> = <span class="variable">$_GET</span>[<span class="string">&#x27;name&#x27;</span>] ?? <span class="string">&#x27;&#x27;</span>;</span><br><span class="line"><span class="keyword">echo</span> <span class="string">&quot;Hello, &quot;</span> . <span class="variable">$name</span>;</span><br></pre></td></tr></table></figure>

<p>看到这种代码，第一反应应该是：</p>
<ul>
<li>输入点在 <code>name</code></li>
<li>来源是 <code>GET</code></li>
<li>输出点在 <code>echo</code></li>
<li>如果没有过滤，就要考虑 XSS、注入、路径拼接等问题</li>
</ul>
<p>除了变量入口，<code>PHP Web</code> 题还经常围绕这些点展开：</p>
<ul>
<li>字符串拼接</li>
<li>文件包含</li>
<li>文件读取</li>
<li>命令执行</li>
<li>反序列化</li>
<li>上传与解析</li>
<li>弱比较</li>
</ul>
<p>常见危险函数和敏感点，建议先认识名字：</p>
<ul>
<li><code>eval</code></li>
<li><code>system</code></li>
<li><code>exec</code></li>
<li><code>passthru</code></li>
<li><code>include</code></li>
<li><code>require</code></li>
<li><code>file_get_contents</code></li>
<li><code>unserialize</code></li>
<li><code>md5</code></li>
<li><code>strcmp</code></li>
</ul>
<p>这里不展开每个漏洞，只强调一个做题思路：</p>
<ul>
<li>先找输入点</li>
<li>再找过滤点</li>
<li>再找危险操作点</li>
<li>最后看数据能不能从输入流到危险点</li>
</ul>
<p>也就是典型的“source -&gt; sanitize -&gt; sink”。</p>
<h2 id="Cookie-与-Session：很多登录题，本质是状态题"><a href="#Cookie-与-Session：很多登录题，本质是状态题" class="headerlink" title="Cookie 与 Session：很多登录题，本质是状态题"></a>Cookie 与 Session：很多登录题，本质是状态题</h2><p><code>HTTP</code> 本身无状态，所以网站需要额外机制记住“你是谁”。</p>
<p>最常见的就是：</p>
<ul>
<li><code>Cookie</code>：状态标识存浏览器</li>
<li><code>Session</code>：状态数据存服务端，浏览器通常只存一个 <code>Session ID</code></li>
</ul>
<p>常见响应头示例：</p>
<figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="attribute">Set-Cookie</span><span class="punctuation">: </span>PHPSESSID=abc123; Path=/; HttpOnly</span><br></pre></td></tr></table></figure>

<p>这里最重要的不是死记属性，而是知道它们在题里意味着什么：</p>
<ul>
<li><code>PHPSESSID=abc123</code>：这是身份标识，不一定是身份本身</li>
<li><code>HttpOnly</code>：前端 JS 不容易直接读到</li>
<li><code>Secure</code>：通常只在 HTTPS 里发</li>
<li><code>Path</code>、<code>Domain</code>：决定 Cookie 作用范围</li>
</ul>
<p>做 <code>Session</code> 相关题时，经常要问自己：</p>
<ul>
<li>登录成功后，多了哪个 Cookie</li>
<li>修改 Cookie 后，权限有没有变化</li>
<li>是不是把身份信息直接放在 Cookie 里了</li>
<li>后端是不是只校验了“有无 Session”，没校验“Session 对应谁”</li>
<li>URL 参数里有没有直接带会话标识</li>
</ul>
<p>如果题目涉及登录、管理员、购物车、验证码、个人中心，基本都值得先抓包看看：</p>
<ul>
<li>有没有 <code>Set-Cookie</code></li>
<li>有没有 <code>remember me</code></li>
<li>有没有 JWT</li>
<li>有没有明显的用户编号、角色编号</li>
</ul>
<h2 id="Linux-常用命令：不是系统运维，是为了更快定位线索"><a href="#Linux-常用命令：不是系统运维，是为了更快定位线索" class="headerlink" title="Linux 常用命令：不是系统运维，是为了更快定位线索"></a>Linux 常用命令：不是系统运维，是为了更快定位线索</h2><p>做 <code>Web</code> 题时，不需要把 Linux 学成运维，但下面这些命令很常用。</p>
<p>查看文件和目录：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">pwd</span></span><br><span class="line"><span class="built_in">ls</span> -la</span><br><span class="line"><span class="built_in">cd</span> /var/www/html</span><br><span class="line">tree</span><br></pre></td></tr></table></figure>

<p>看文件内容：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cat</span> file.txt</span><br><span class="line">less access.log</span><br><span class="line"><span class="built_in">head</span> -n 20 index.php</span><br><span class="line"><span class="built_in">tail</span> -n 50 error.log</span><br></pre></td></tr></table></figure>

<p>搜索关键词：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grep -R <span class="string">&quot;flag&quot;</span> .</span><br><span class="line">grep -R <span class="string">&quot;password&quot;</span> .</span><br><span class="line">find . -name <span class="string">&quot;*.php&quot;</span></span><br><span class="line">find . -name <span class="string">&quot;*.bak&quot;</span></span><br></pre></td></tr></table></figure>

<p>网络与请求：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl http://target/path</span><br><span class="line">wget http://target/file</span><br></pre></td></tr></table></figure>

<p>权限与环境：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">whoami</span></span><br><span class="line"><span class="built_in">id</span></span><br><span class="line"><span class="built_in">uname</span> -a</span><br><span class="line"><span class="built_in">env</span></span><br></pre></td></tr></table></figure>

<p>对 <code>CTF Web</code> 来说，最实用的不是“命令有多少”，而是知道什么时候用：</p>
<ul>
<li>找源码用 <code>find</code></li>
<li>找敏感字符串用 <code>grep</code></li>
<li>看日志用 <code>tail</code></li>
<li>试接口用 <code>curl</code></li>
<li>看当前用户和权限用 <code>whoami</code>、<code>id</code></li>
</ul>
<h2 id="Docker：很多-Web-靶机其实跑在容器里"><a href="#Docker：很多-Web-靶机其实跑在容器里" class="headerlink" title="Docker：很多 Web 靶机其实跑在容器里"></a>Docker：很多 Web 靶机其实跑在容器里</h2><p>现在很多题目环境不是直接装在系统上，而是跑在 <code>Docker</code> 容器里。对做题的人来说，不需要一开始就把 <code>Docker</code> 学得很深，但至少要知道它会影响你怎么看端口、源码、日志和服务关系。</p>
<p>先记住 3 个概念：</p>
<ul>
<li><code>image</code>：镜像，可以理解成“运行模板”</li>
<li><code>container</code>：容器，是镜像跑起来后的实例</li>
<li><code>volume / bind mount</code>：挂载，把宿主机目录映射进容器</li>
</ul>
<p>如果一道题给了源码、<code>Dockerfile</code>、<code>docker-compose.yml</code> 或者启动脚本，那它通常在告诉你两件事：</p>
<ul>
<li>服务是怎么跑起来的</li>
<li>关键文件和端口在哪里</li>
</ul>
<p>做 <code>CTF Web</code> 时最常见的 Docker 线索有：</p>
<ul>
<li><code>ports</code></li>
<li><code>volumes</code></li>
<li><code>environment</code></li>
<li><code>Dockerfile</code></li>
<li><code>docker-compose.yml</code></li>
<li>多个服务名，比如 <code>web</code>、<code>php</code>、<code>mysql</code>、<code>redis</code></li>
</ul>
<p>例如下面这个片段：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">web:</span></span><br><span class="line">    <span class="attr">build:</span> <span class="string">.</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;8080:80&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./src:/var/www/html</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">FLAG=ctf&#123;example&#125;</span></span><br></pre></td></tr></table></figure>

<p>看到它时，至少该立刻想到：</p>
<ul>
<li>宿主机访问端口是 <code>8080</code></li>
<li>容器内服务真实监听的可能是 <code>80</code></li>
<li>源码目录大概率在 <code>/var/www/html</code></li>
<li>环境变量里可能有敏感信息</li>
</ul>
<p>最常用的 Docker 命令，先会这几个就够用了：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker ps</span><br><span class="line">docker images</span><br><span class="line">docker logs &lt;container&gt;</span><br><span class="line">docker <span class="built_in">exec</span> -it &lt;container&gt; /bin/sh</span><br><span class="line">docker inspect &lt;container&gt;</span><br><span class="line">docker <span class="built_in">cp</span> &lt;container&gt;:/path/in/container ./local_dir</span><br></pre></td></tr></table></figure>

<p>它们分别常用来做什么：</p>
<ul>
<li><code>docker ps</code>：看当前跑了哪些容器、映射了哪些端口</li>
<li><code>docker logs</code>：看报错、启动输出、调试信息</li>
<li><code>docker exec -it</code>：进容器里看文件、跑命令、摸目录</li>
<li><code>docker inspect</code>：看挂载、网络、环境变量、启动参数</li>
<li><code>docker cp</code>：把容器里的源码、日志、配置复制出来</li>
</ul>
<p>如果能进入容器，通常优先看这些位置：</p>
<ul>
<li>Web 根目录，如 <code>/var/www/html</code></li>
<li>配置文件，如 <code>nginx.conf</code>、<code>apache2.conf</code></li>
<li>PHP 配置和站点配置</li>
<li>日志目录</li>
<li><code>/app</code>、<code>/srv</code>、<code>/usr/src/app</code> 这种项目目录</li>
</ul>
<p>做题时，Docker 最容易帮你解决的几个问题是：</p>
<ul>
<li>“服务到底监听在哪个端口上”</li>
<li>“源码到底在容器里的哪个目录”</li>
<li>“这个文件是宿主机原生文件，还是挂载进去的”</li>
<li>“多服务之间怎么通信”</li>
<li>“环境变量里有没有账号、密码、flag 或密钥”</li>
</ul>
<p>如果题目给了 <code>Dockerfile</code>，建议重点扫这几类指令：</p>
<ul>
<li><code>FROM</code>：基础镜像，能看出技术栈</li>
<li><code>COPY</code> &#x2F; <code>ADD</code>：源码和配置被拷到哪</li>
<li><code>RUN</code>：安装了什么依赖，做了什么初始化</li>
<li><code>EXPOSE</code>：容器内声明了哪个端口</li>
<li><code>CMD</code> &#x2F; <code>ENTRYPOINT</code>：容器启动时真正执行什么</li>
</ul>
<p>如果题目给了 <code>docker-compose.yml</code>，建议重点看：</p>
<ul>
<li>哪些服务会一起启动</li>
<li>哪个服务对外开放端口</li>
<li>数据库用户名、密码、库名</li>
<li>挂载目录对应到哪里</li>
<li>服务之间是不是通过服务名互联</li>
</ul>
<p>一句话概括：<code>Docker</code> 在 <code>CTF Web</code> 里不只是“部署工具”，它本身就是环境信息说明书。</p>
<h2 id="一套适合新手的-Web-题排查顺序"><a href="#一套适合新手的-Web-题排查顺序" class="headerlink" title="一套适合新手的 Web 题排查顺序"></a>一套适合新手的 Web 题排查顺序</h2><p>如果你拿到一道题暂时没思路，可以先按这个顺序走：</p>
<ol>
<li>先看页面功能，猜题目可能考什么。</li>
<li>打开开发者工具，看源码、JS、隐藏字段、接口路径。</li>
<li>抓一遍正常请求，记住参数名、请求方法、Cookie、响应头。</li>
<li>对输入点做最小测试，看回显、报错、状态码、响应长度是否变化。</li>
<li>怀疑编码时，先解码再继续看，不要直接乱打 payload。</li>
<li>如果后端像 PHP，就重点关注 <code>$_GET</code>、<code>$_POST</code>、<code>$_COOKIE</code>、<code>$_SESSION</code>。</li>
<li>如果涉及登录态，就优先研究 Cookie、Session、权限标识。</li>
<li>如果题目带源码或部署文件，就顺手检查 <code>Dockerfile</code>、<code>docker-compose.yml</code> 和端口映射。</li>
</ol>
<p>很多题卡住，不是因为完全不会，而是把顺序走反了。</p>
<h2 id="这篇速查最该带走的几点"><a href="#这篇速查最该带走的几点" class="headerlink" title="这篇速查最该带走的几点"></a>这篇速查最该带走的几点</h2><ul>
<li><code>fragment</code> 不会发给服务器，<code>query</code> 会。</li>
<li><code>Set-Cookie</code> 和 <code>Cookie</code> 是两件事，一个是服务端下发，一个是客户端回传。</li>
<li>前端隐藏参数不等于安全参数。</li>
<li>看不懂字符串时，优先怀疑编码，而不是优先怀疑加密。</li>
<li>做 PHP 题，先找输入点，再找危险点。</li>
<li>很多“权限题”本质上是“状态题”。</li>
<li>很多靶机线索不在页面里，而在 <code>Dockerfile</code>、挂载和环境变量里。</li>
</ul>
]]></content>
      <categories>
        <category>Web安全</category>
      </categories>
      <tags>
        <tag>Web安全</tag>
        <tag>CTF</tag>
        <tag>HTTP</tag>
        <tag>PHP</tag>
      </tags>
  </entry>
</search>
